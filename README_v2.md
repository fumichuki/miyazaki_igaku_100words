# 鹿児島大学英作文特訓システム v2.0

## 🎯 概要

鹿児島大学の入試英作文対策に特化したAI搭載学習システムです。GPT-4oを活用し、**獣医学部モード**と**理系・文系モード**の2つの出題形式に対応しています。

### バージョン情報
- **Version**: 2.0.0
- **実装完了日**: 2026-01-18
- **実装フェーズ**: Phase 1-3完了、Phase 4リファクタリング済み

## ✨ 主要機能

### 1. 問題生成 (Question Generation)
- **アーキタイプベース生成** (Phase 2)
  - 10種類の獣医学部アーキタイプ
  - 31種類の専門トピック
  - 「two reasons/things/suggestions」を100%保証
- **LLMフリー生成** (従来方式)
  - GPT-4oによる自由な問題生成
  - テーマ重複回避機能

### 2. 添削機能 (Correction & Scoring)
- **5項目採点システム**
  - 内容 (Content): 5点
  - 構成 (Structure): 5点
  - 語彙 (Vocabulary): 5点
  - 文法 (Grammar): 5点
  - 語数 (Word Count): 5点
  - **合計**: 25点満点

- **制約チェック** (Phase 1)
  - 語数カウント（正確な単語数測定）
  - Two-units検証（2つの理由/提案の確認）
  - 信頼度レベル（high/medium/low）

### 3. アウトライン支援 (Outline Support) - Phase 3
- セクション別の構造ガイド
- 各セクションの目的・例文・キーポイント
- 語数推定機能
- アーキタイプ連携カスタマイズ

## 🚀 クイックスタート

### 1. インストール

```bash
# リポジトリをクローン
git clone <repository-url>
cd eisakubun-kagoshima

# 依存パッケージをインストール
pip install -r requirements.txt

# 環境変数を設定
cp .env.example .env
# .envファイルにOPENAI_API_KEYを設定
```

### 2. サーバー起動

```bash
python3 app.py
```

サーバーは http://localhost:8002 で起動します。

### 3. 動作確認

```bash
# ヘルスチェック
curl http://localhost:8002/health

# 問題生成（獣医学部モード）
curl -X POST http://localhost:8002/api/question \
  -H "Content-Type: application/json" \
  -d '{"mode": "veterinary"}'
```

## 📚 API エンドポイント

### 1. 問題生成 - `POST /api/question`

**リクエスト:**
```json
{
  "mode": "veterinary"  // or "general"
}
```

**レスポンス:**
```json
{
  "theme": "Preventive Veterinary Care",
  "japanese_sentences": ["..."],
  "hints": [...],
  "target_words": {"min": 100, "max": 150},
  "archetype_id": "A1",
  "topic_id": "preventive_care",
  "required_units": 2,
  "unit_type": "reasons",
  "question_text_english": "Do you think... Give two reasons."
}
```

### 2. 添削 - `POST /api/correct`

**リクエスト:**
```json
{
  "theme": "Preventive Care",
  "question": "予防医療について...",
  "answer": "Pet owners should...",
  "mode": "veterinary"
}
```

**レスポンス:**
```json
{
  "corrected": "改善された英文",
  "score": {
    "content": 4,
    "structure": 5,
    "vocabulary": 4,
    "grammar": 4,
    "word_count_penalty": 0
  },
  "total": 21,
  "feedback": "詳細なフィードバック",
  "word_count": 95,
  "constraint_checks": {
    "word_count_actual": 95,
    "word_count_is_valid": true,
    "two_units_detected_count": 2,
    "two_units_is_valid": true,
    "confidence": "high"
  }
}
```

### 3. 制約チェック - `POST /api/validate-constraints`

**リクエスト:**
```json
{
  "answer": "英文...",
  "target_min": 80,
  "target_max": 120,
  "mode": "veterinary"
}
```

**レスポンス:**
```json
{
  "constraint_checks": {
    "word_count_actual": 95,
    "word_count_is_valid": true,
    "two_units_detected_count": 2,
    "two_units_is_valid": true
  },
  "ready_to_submit": true
}
```

### 4. アウトライン生成 - `POST /api/outline`

**リクエスト:**
```json
{
  "theme": "Preventive Care",
  "question_type": "reasons",
  "required_units": 2,
  "mode": "veterinary",
  "target_words": {"min": 80, "max": 120}
}
```

**レスポンス:**
```json
{
  "theme": "Preventive Care",
  "structure_type": "2-reasons",
  "sections": [
    {
      "label": "Introduction",
      "purpose": "State your opinion...",
      "suggested_sentences": 2,
      "key_points": ["...", "..."],
      "example_phrases": ["...", "..."]
    },
    ...
  ],
  "tips": ["...", "..."],
  "estimated_words": 150
}
```

## 🏗️ システムアーキテクチャ

### ディレクトリ構成

```
eisakubun-kagoshima/
├── app.py                      # Flask API サーバー
├── config.py                   # 設定管理 (NEW)
├── models.py                   # Pydanticスキーマ定義
├── llm_service.py              # OpenAI API 呼び出し
├── database.py                 # SQLite データベース管理
├── prompts_veterinary.py       # 獣医学部用プロンプト
│
├── constraint_validator.py     # Phase 1: 制約チェック
├── archetype_templates.py      # Phase 2: アーキタイプ定義
├── question_generator.py       # Phase 2: 問題生成エンジン
├── outline_generator.py        # Phase 3: アウトライン生成
│
├── data/
│   ├── kagoshima_eisakubun.db # SQLiteデータベース
│   └── past_questions.json    # 過去問データ
│
├── templates/
│   └── index.html             # Webインターフェース
├── static/
│   ├── style.css
│   └── main.js
│
└── tests/
    ├── test_constraints.py    # Phase 1 テスト
    ├── test_archetypes.py     # Phase 2 テスト
    ├── test_outline.py        # Phase 3 テスト
    ├── test_api_phase1.py     # Phase 1 API統合
    ├── test_api_phase2.py     # Phase 2 API統合
    └── test_api_phase3.py     # Phase 3 API統合
```

### データフロー

```
┌─────────────┐
│  Frontend   │ (templates/index.html)
└──────┬──────┘
       │ HTTP POST
       ↓
┌─────────────────────────────────────────┐
│         Flask API (app.py)              │
├─────────────────────────────────────────┤
│ • /api/question    → generate_question  │
│ • /api/correct     → correct_answer     │
│ • /api/validate    → validate_constraints│
│ • /api/outline     → generate_outline   │
└──────┬──────────────┬───────────────────┘
       │              │
       ↓              ↓
┌──────────────┐  ┌──────────────┐
│ LLM Service  │  │ Validators   │
│ (OpenAI GPT) │  │ & Generators │
└──────┬───────┘  └──────┬───────┘
       │                 │
       ↓                 ↓
┌───────────────────────────────┐
│  Database (SQLite)            │
│  • questions                  │
│  • submissions                │
│  • used_themes                │
└───────────────────────────────┘
```

## 📋 実装フェーズ詳細

### Phase 1: 制約チェック ✅
- **目的**: LLMに依存しない確定的な検証
- **実装内容**:
  - `constraint_validator.py`: 語数カウント、two-units検証
  - `/api/validate-constraints`: 専用エンドポイント
  - 信頼度レベル（high/medium/low）
- **テスト**: 33ユニットテスト + 5統合テスト

### Phase 2: Archetype-based Generation ✅
- **目的**: 「two X」を100%保証する問題生成
- **実装内容**:
  - `archetype_templates.py`: 10アーキタイプ + 31トピック
  - `question_generator.py`: テンプレートベース生成
  - 正規表現による検証
  - フォールバックチェーン（Archetype → LLM → Hardcoded）
- **テスト**: 38ユニットテスト

### Phase 3: Outline Support ✅
- **目的**: 学生の構成力向上支援
- **実装内容**:
  - `outline_generator.py`: セクション別ガイド生成
  - アーキタイプ連携カスタマイズ
  - 語数推定機能
- **テスト**: 6グループ + 7API統合テスト

### Phase 4: Refactoring ✅
- **目的**: 保守性・拡張性の向上
- **実装内容**:
  - `config.py`: 一元的な設定管理
  - ハードコード値の排除
  - ロギング改善
  - ドキュメント整備

## 🧪 テスト

### すべてのテストを実行

```bash
# Phase 1
python3 test_constraints.py

# Phase 2
python3 test_archetypes.py

# Phase 3
python3 test_outline.py

# API統合テスト
python3 test_api_phase1.py
python3 test_api_phase2.py
python3 test_api_phase3.py
```

### テスト結果サマリー

```
Phase 1: ✅ 38/38 tests passed
Phase 2: ✅ 38/38 tests passed
Phase 3: ✅ 13/13 tests passed
Total:   ✅ 89/89 tests passed
```

## ⚙️ 設定

### 環境変数 (.env)

```bash
# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_RETRIES=3

# Server
HOST=0.0.0.0
PORT=8002
DEBUG=true
LOG_LEVEL=INFO

# CORS
CORS_ORIGINS=*
```

### config.py による設定

設定値は `config.py` で一元管理されています：

- **語数設定**: `GENERAL_WORD_COUNT`, `VETERINARY_WORD_COUNT`
- **アーキタイプ優先度**: `USE_ARCHETYPE_BY_DEFAULT`
- **スコアリング**: `SCORING_WEIGHTS`
- **機能フラグ**: `FEATURES`

## 📊 統計情報

### コード規模

```
総コード行数: 5,181行
実装ファイル: 12個
テストファイル: 7個
ドキュメント: 4個
```

### アーキタイプ・トピック

```
獣医学部アーキタイプ: 10種類
獣医学部トピック: 31種類
理系・文系アーキタイプ: 4種類
理系・文系トピック: 20種類
総組み合わせ数: 390通り
```

## 🐛 既知の問題

1. **LLMフォールバック**: アーキタイプ生成失敗時、LLMは「two X」を保証しない
2. **データベーススキーマ**: アーキタイプメタデータの永続化は未実装
3. **レート制限**: API呼び出し制限は未実装（設定のみ準備済み）

## 🚧 今後の拡張案

- [ ] アーキタイプメタデータのDB保存
- [ ] ユーザー認証・セッション管理
- [ ] 学習履歴の可視化
- [ ] 多言語対応
- [ ] レート制限の実装
- [ ] キャッシング機能

## 📝 ライセンス

このプロジェクトは教育目的で開発されています。

## 👥 開発者

- 鹿児島大学英作文特訓システム開発チーム

## 📞 サポート

問題が発生した場合は、Issueを作成してください。

---

**Version**: 2.0.0  
**Last Updated**: 2026-01-18
